#include <iostream>

using namespace std;
int main(int argc, char const *argv[]) {
  cin.tie(0);
  ios::sync_with_stdio(false);
  int x, y;
  cin >> x;
  cin >> y;

  cout << x + y / 2 << "\n";
  return 0;
}